# Core
pyyaml>=6.0
pydantic>=2.5
pandas>=2.0
numpy>=1.24
python-dateutil>=2.8
pytz>=2023.3

# HTTP / scraping
httpx>=0.27
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
trafilatura>=1.8
readability-lxml>=0.8.1
feedparser>=6.0
tldextract>=5.1
rapidfuzz>=3.5

# Storage / export
pyarrow>=14.0

# Dashboard
streamlit>=1.30
plotly>=5.18

# Optional browser rendering (installed on demand)
# playwright>=1.40
# camoufox[geoip]>=0.4

# Optional LLM
# openai>=1.10

# Optional Hive sink (eBay / Krylov only) — install on Krylov via:
#   pip install --user --proxy http://httpproxy-tcop.vip.ebay.com:80 'pyhive[hive]' thrift thrift_sasl sasl
# pyhive[hive]>=0.7

# Env
python-dotenv>=1.0

# Tests
pytest>=7.4
